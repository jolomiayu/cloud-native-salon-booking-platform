apiVersion: v1
kind: Service
metadata:
  name: {{ include "salon-booking-chart.fullname" . }}
  labels:
    {{- include "salon-booking-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "salon-booking-chart.selectorLabels" . | nindent 4 }}
