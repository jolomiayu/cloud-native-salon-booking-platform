apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "salon-booking-chart.fullname" . }}-test-connection"
  labels:
    {{- include "salon-booking-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "salon-booking-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
